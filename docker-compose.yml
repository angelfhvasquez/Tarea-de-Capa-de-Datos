version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: petcenter_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: petcenter_db
      MYSQL_USER: petcenter
      MYSQL_PASSWORD: petcenter123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - petcenter_network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: petcenter_backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/petcenter_db
      SPRING_DATASOURCE_USERNAME: petcenter
      SPRING_DATASOURCE_PASSWORD: petcenter123
    ports:
      - "8080:8080"
    depends_on:
      - mysql
    networks:
      - petcenter_network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: petcenter_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - petcenter_network

volumes:
  mysql_data:

networks:
  petcenter_network:
    driver: bridge
